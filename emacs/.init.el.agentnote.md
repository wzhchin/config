# Emacs 配置性能审计决策记录

- 2026-08-25
  - Problem: 当前 Emacs 配置经过“最小启动”和 Emacs 31 迁移，但性能取舍缺少运行时基线；仓库还保留一批未接入当前入口的旧模块。
  - Decision: 当前生效边界按 `early-init.el`、`init.el` 和首个 idle timer 加载的 `chin/chin-basic.el` 处理。`chin-ui`、`chin-file`、`chin-completion`、`chin-lsp`、`chin-org`、`chin-project` 等旧模块在确认实际运行实例会加载前，不作为当前启动热点重构。
  - Decision: 优先核查 `emacs/init.el:95-100` 的字节编译路径。source 在 `chin/chin-basic.el`，compiled 却指向配置根目录的 `chin-basic.elc`；正常同目录编译产物应在 `chin/chin-basic.elc`，因此当前编译收益可能不会命中。
  - Decision: 不凭感觉改掉手工 package load-path 扫描、`package-quickstart`、`gc-cons-threshold` 或 0.05 秒 idle timer。先做同环境 A/B，记录冷启动、首帧、首个 idle 回调、首个按键、`gcs-done`、`gc-elapsed` 和内存峰值。
  - Decision: 若首个按键受 deferred load 影响，优先拆分 `evil`/基础键位与 `vertico`/`point-stack` 等功能，或延后次要功能；单纯把 0.05 秒改大不是默认方案。
  - Decision: 旧模块中的全局 git-gutter、行号、失焦保存、项目根检测、Org/Dired 外部进程和 Eglot 启动，只在确认重新进入入口后分别 profile；它们不能从“文件存在”推断为当前冷启动成本。
  - Evidence: `f17ff09` 解释模块拆分；`1749bbd` 解释最小启动和手工包路径；`9e9d988` 解释首帧后的延迟加载；`c4d48e3` 解释 Emacs 31 的 `setopt treesit-enabled-modes` 与 load-path 目录缓存。
  - Scope: 本次只读审计未修改已有 Emacs 配置；保留工作树中原有改动和审计日志。
  - Verification: `git diff --check` 被工作树原有的 `ai-agent/RULES.md:24` 尾随空格阻断；该文件不属于本次范围，未清理。

- 2026-08-25
  - Decision: 按 P0 方案修正 `emacs/init.el` 的编译产物路径，改为从 source 路径派生 `chin/chin-basic.elc`，避免正常同目录编译产物永远不被命中。
  - Verification: `check-parens` 通过；路径断言得到 `/home/chin/files/config/emacs/chin/chin-basic.elc`；`git diff --check -- emacs/init.el` 通过；未生成 `.elc` 或 `.eln` 文件。

- 2026-08-25
  - Problem: 首次批量启动探针因 shell 中嵌套 Emacs Lisp 单引号导致 zsh 解析失败。
  - Tried method: 直接把带引号的 `--eval` 表达式嵌在单引号 shell 命令中。
  - Fix: 改用外层双引号传递 `--eval`，并显式转义 Emacs Lisp 字符串；随后实际加载配置验证通过。

- 2026-08-25
  - Problem: 启用 Git Gutter 后出现 `git-gutter-mode--set-explicitly` 未绑定。
  - Evidence: Emacs 31.1 的 `define-globalized-minor-mode` 生成双连字符内部变量；已安装 `git-gutter.elc` 仍包含旧的单连字符变量，而直接加载同目录 source 会生成双连字符变量。
  - Tried method: 在 `emacs -Q` 下分别加载 stale `.elc`、source、完整配置，并测试新文件缓冲区；完整配置和 source 路径均通过，混用编译产物才暴露版本不一致风险。
  - Decision: 不在配置中伪造 `git-gutter` 私有变量；在 Emacs 31.1 下重新编译已安装的 `git-gutter` 包并重启 Emacs。保留 `init.el` 的 Git Gutter 逻辑不变。

- 2026-08-25
  - Decision: 将交互配置拆为 `chin-start.el`、`chin-first-second.el` 和 `chin-first-call.el`；核心 Evil/leader keymap 立即加载，which-key 与 Git Gutter 在 1 秒 idle 后加载，其余交互配置在首个命令前加载。
  - Rejected: 单独的 `chin-first-press.el`；捕获首个物理按键会引入输入翻译复杂度，而 which-key 延迟到 1 秒已符合当前要求。
  - Evidence: 当前 Emacs 31.1 已内置 `which-key.el.gz`。
  - Decision: 不把 `which-key` 加入 ELPA-only 包路径清单，直接在 `chin-first-second.el` 中 `require` 并启用。
